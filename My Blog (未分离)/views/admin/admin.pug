//- 后台界面重写
doctype html
html
  head
    title 后台管理
    meta(charset="utf-8")
    meta(name="author", content="bigbao")
    link(rel="stylesheet", href="/layui/css/layui.css")
    link(rel="stylesheet", href="/css/nav.css")
    script(src="/layui/layui.js")

    style.
      .layui-side, .layui-body{
        top: 60px;
      }
      .layui-body{
        padding: 15px;
      }
      

  body
    // 导航
    .blog-header.layui-bg-black
      .layui-container
        ul.layui-nav
          li.layui-nav-item#haha
            a(href="/")
              i.layui-icon.layui-icon-home 
              | 首页
        //- 用户登录按钮
        ul.layui-nav.blog-user
          li.layui-nav-item
            a(href="/user/logout") 退出
    // 左侧导航
    .layui-side.layui-bg-black
      .layui-side-scroll
        ul(lay-filter="list").layui-nav.layui-nav-tree
          if role > 1
            li.layui-nav-item
              a(href="/admin/user") 用户管理
          li.layui-nav-item
            a(href="/admin/article") 日志管理
          if role > 1
            li.layui-nav-item
              a(href="/admin/text") 文章管理
          li.layui-nav-item
            a(href="/admin/comment") 日志评论管理
          li.layui-nav-item
            a(href="/admin/comment1") 文章评论管理
          li.layui-nav-item
            a(href="/admin/message") 留言管理
          li.layui-nav-item
            a(href="/admin/album") 相册管理
          li.layui-nav-item
            a(href="/admin/userface") 头像上传
    // 后台管理主体部分
    .layui-body
      block main
    
    script.
      layui.use('element', function(){
        var element = layui.element;
        const $ = layui.$

        $("a").each((i, v) => {
          const a_href = $(v).prop('href')

          
          if(a_href === location.href){
            $('li').eq(i).addClass("layui-this")
          }
          
        })
      });